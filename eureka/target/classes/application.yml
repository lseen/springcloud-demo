#设置项目启动名称及开启eureka服务端的ip、端口、注册方式
#1.eureka注册中心
eureka:
  instance:
    hostname: 127.0.0.1
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://${eureka.instance.hostname}:${server.port}
spring:
  application:
    name: eureka-server

